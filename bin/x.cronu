#!/usr/bin/env bash

crontab_0x1user_local="$HOME/.0x1/00mu/cron/00config/crontab.0x1local"

if [ -f $crontab_0x1user_local ]; then
    cat $crontab_0x1user_local | crontab -
fi

x.cronl

# vim: ft=sh
